@model MvcLinqSql.Models.area

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>area</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.area_des)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.area_des)
            @Html.ValidationMessageFor(model => model.area_des)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
